"use client";

import React from 'react';
import { BackButton as ModernBackButton } from '@/components/ui/Buttons';

// Re-export our modern buttons with the same interface for backward compatibility
export const BackButton = ModernBackButton;
